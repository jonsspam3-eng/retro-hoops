/**
 * HOW TO EDIT THIS FILE
 * - Update this object to control brand identity across the site.
 * - `siteName`: used in metadata, headings, and logo fallback text.
 * - `logoPath`: path to your logo file inside `/public` (example: `/images/logo/site-logo.svg`).
 * - `primaryColor`, `backgroundColor`, `textColor`: global brand colors.
 * - `defaultTheme`: choose `"dark"` or `"light"` for first load.
 */
export const brandConfig = {
  siteName: "archive_13",
  logoPath: "/images/logo/site-logo.svg",
  primaryColor: "#a13a3a",
  backgroundColor: "#0a0a0a",
  textColor: "#f2f2f2",
  defaultTheme: "dark",
};
